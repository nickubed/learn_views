<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nick is Cool</title>
</head>
<body>
    <h2>Did you know that <%= name %> is <%= age * 7 %> dog years old??</h2>
    <% var status %>
    <% if (age > 100) { %>
        <% status = 'old' %>
    <% } else { %>
        <% status = 'not old' %>
    <% } %>
    <h3><%= name %> is sooooo <%= status %>.</h3>
</body>
</html>